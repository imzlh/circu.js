{
  "name": "circu.js",
  "version": "0.0.0",
  "description": "Utilities for circu.js",
  "private": true,
  "scripts": {
    "api-docs": "typedoc --tsconfig tsconfig.json",
    "api-docs-watch": "typedoc --tsconfig tsconfig.json --watch --preserveWatchOutput --logLevel Verbose",
    "deploy-api-docs": "gh-pages -d api -e api -u \"github-actions-bot <support+actions@github.com>\""
  },
  "license": "MIT",
  "dependencies": {
    "gh-pages": "^6.3.0"
  },
  "devDependencies": {
    "typedoc": "0.28.19"
  }
}
